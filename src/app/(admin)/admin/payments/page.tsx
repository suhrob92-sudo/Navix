import { AdminPaymentsContent } from '@/app/(admin)/admin/payments/admin-payments-content';

export const metadata = {
  title: "To'lovlar — Admin",
  description: "Xizmat to'lovlarini qidirish va pulni qaytarish.",
};

export default function AdminPaymentsPage() {
  return <AdminPaymentsContent />;
}
