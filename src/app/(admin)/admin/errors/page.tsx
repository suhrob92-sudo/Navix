import { AdminErrorsContent } from '@/app/(admin)/admin/errors/errors-content';

export const metadata = {
  title: 'Xatolar — Admin',
  description: 'Ilovada yuz bergan xatolar jurnali.',
};

export default function AdminErrorsPage() {
  return <AdminErrorsContent />;
}
