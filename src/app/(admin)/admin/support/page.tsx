import { AdminSupportContent } from '@/app/(admin)/admin/support/support-content';

export const metadata = {
  title: 'Murojaatlar — Admin',
  description: 'Foydalanuvchilar murojaatlariga javob berish.',
};

export default function AdminSupportPage() {
  return <AdminSupportContent />;
}
