import { NewProviderContent } from '@/app/(admin)/admin/providers/new/new-provider-content';

export const metadata = {
  title: 'Yangi xizmat — Admin',
  description: "To'lov qabul qiluvchi yangi xizmat qo'shish.",
};

export default function NewProviderPage() {
  return <NewProviderContent />;
}
