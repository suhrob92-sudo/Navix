import { AdminProvidersContent } from '@/app/(admin)/admin/providers/providers-content';

export const metadata = {
  title: 'Xizmatlar — Admin',
  description: "To'lov qabul qiluvchi xizmatlarni boshqarish.",
};

export default function AdminProvidersPage() {
  return <AdminProvidersContent />;
}
