import { AdminAuditContent } from '@/app/(admin)/admin/audit/audit-content';

export const metadata = {
  title: 'Audit jurnali — Admin',
  description: 'Kim, qachon, nima qildi.',
};

export default function AdminAuditPage() {
  return <AdminAuditContent />;
}
