import { AdminModulesContent } from '@/app/(admin)/admin/modules/modules-content';

export const metadata = {
  title: "Bo'limlar — Admin",
  description: "Bo'limlarni vaqtincha yopish va qayta ochish.",
};

export default function AdminModulesPage() {
  return <AdminModulesContent />;
}
