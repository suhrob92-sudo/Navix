import { AdminContentModerationContent } from '@/app/(admin)/admin/content/content-content';

export const metadata = {
  title: 'Kontent — Admin',
  description: 'Mahsulot, taom, post va vakansiyalarni yashirish.',
};

export default function AdminContentPage() {
  return <AdminContentModerationContent />;
}
