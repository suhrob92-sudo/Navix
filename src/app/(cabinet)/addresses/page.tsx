import { AddressesContent } from '@/app/(cabinet)/addresses/addresses-content';

export const metadata = {
  title: 'Manzillarim',
  description: 'Uy, ish va boshqa manzillaringizni saqlang.',
};

export default function AddressesPage() {
  return <AddressesContent />;
}
