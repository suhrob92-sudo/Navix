import { TravelContent } from '@/app/(cabinet)/travel/travel-content';

export const metadata = {
  title: 'Sayohat',
  description: 'Samolyot, poyezd va avtobus chiptalari bitta qidiruvda.',
};

export default function TravelPage() {
  return <TravelContent />;
}
