import { TicketsContent } from '@/app/(cabinet)/travel/tickets/tickets-content';

export const metadata = {
  title: 'Chiptalarim',
  description: 'Sotib olingan chiptalar va bekor qilish.',
};

export default function TicketsPage() {
  return <TicketsContent />;
}
