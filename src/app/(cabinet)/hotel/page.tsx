import { HotelContent } from '@/app/(cabinet)/hotel/hotel-content';

export const metadata = {
  title: 'Mehmonxona',
  description: 'Xonalarni jonli narxlar bilan band qiling.',
};

export default function HotelPage() {
  return <HotelContent />;
}
