import { DashboardContent } from '@/app/(cabinet)/dashboard/dashboard-content';

export const metadata = {
  title: 'Kabinet',
  description: 'Shaxsiy kabinetingiz — barcha xizmatlar bir joyda.',
};

export default function DashboardPage() {
  return <DashboardContent />;
}
