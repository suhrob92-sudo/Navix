import { MessagesContent } from '@/app/(cabinet)/messages/messages-content';

export const metadata = {
  title: 'Xabarlar',
  description: 'Suhbatlar va kompaniyalar bilan yozishmalar.',
};

export default function MessagesPage() {
  return <MessagesContent />;
}
