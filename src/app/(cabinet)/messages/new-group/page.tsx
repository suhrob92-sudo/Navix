import { NewGroupContent } from '@/app/(cabinet)/messages/new-group/new-group-content';

export const metadata = {
  title: 'Yangi guruh',
  description: "Guruh nomi, rasmi va a'zolarini tanlang.",
};

export default function NewGroupPage() {
  return <NewGroupContent />;
}
