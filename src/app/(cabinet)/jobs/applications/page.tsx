import { ApplicationsContent } from '@/app/(cabinet)/jobs/applications/applications-content';

export const metadata = {
  title: 'Mening arizalarim',
  description: 'Yuborilgan arizalar va ularning holati.',
};

export default function ApplicationsPage() {
  return <ApplicationsContent />;
}
