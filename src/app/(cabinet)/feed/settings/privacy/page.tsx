import { PrivacySettingsContent } from '@/app/(cabinet)/feed/settings/privacy/privacy-settings';

export const metadata = {
  title: 'Maxfiylik',
  description: "Kim ko'radi, kim izoh yozadi, kim obuna bo'ladi.",
};

export default function PrivacySettingsPage() {
  return <PrivacySettingsContent />;
}
