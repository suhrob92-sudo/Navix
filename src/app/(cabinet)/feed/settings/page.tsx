import { FeedSettingsContent } from '@/app/(cabinet)/feed/settings/settings-content';

export const metadata = {
  title: 'Feed sozlamalari',
  description: 'Kontent, maxfiylik va bildirishnoma sozlamalari.',
};

export default function FeedSettingsPage() {
  return <FeedSettingsContent />;
}
