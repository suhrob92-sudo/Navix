import { FeedSearchContent } from '@/app/(cabinet)/feed/search/search-content';

export const metadata = {
  title: 'Feed qidiruvi',
  description: 'Video, yaratuvchi va xeshteglarni qidirish.',
};

export default function FeedSearchPage() {
  return <FeedSearchContent />;
}
