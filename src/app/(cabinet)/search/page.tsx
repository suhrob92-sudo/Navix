import { SearchContent } from '@/app/(cabinet)/search/search-content';

export const metadata = {
  title: 'Qidiruv',
  description: 'Xizmat yoki mahsulot qidiring.',
};

export default function SearchPage() {
  return <SearchContent />;
}
