import { TransferContent } from '@/app/(cabinet)/wallet/transfer/transfer-content';

export const metadata = {
  title: "Pul o'tkazish",
  description: 'Boshqa Navix foydalanuvchisiga pul yuboring.',
};

export default function TransferPage() {
  return <TransferContent />;
}
