import { TopUpContent } from '@/app/(cabinet)/wallet/topup/topup-content';

export const metadata = {
  title: "Hisobni to'ldirish",
  description: 'Hamyoningizga pul kiriting.',
};

export default function TopUpPage() {
  return <TopUpContent />;
}
