import { WalletContent } from '@/app/(cabinet)/wallet/wallet-content';

export const metadata = {
  title: 'Hamyon',
  description: "Balans, to'ldirish va o'tkazmalar.",
};

export default function WalletPage() {
  return <WalletContent />;
}
