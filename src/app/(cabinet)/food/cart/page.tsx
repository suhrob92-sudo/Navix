import { CartContent } from '@/app/(cabinet)/food/cart/cart-content';

export const metadata = {
  title: 'Savat',
  description: 'Buyurtmani tasdiqlash va yetkazish manzili.',
};

export default function CartPage() {
  return <CartContent />;
}
