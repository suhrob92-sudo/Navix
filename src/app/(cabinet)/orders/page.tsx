import { OrdersContent } from '@/app/(cabinet)/orders/orders-content';

export const metadata = {
  title: 'Buyurtmalar',
  description: 'Barcha modullardagi buyurtmalaringiz.',
};

export default function OrdersPage() {
  return <OrdersContent />;
}
