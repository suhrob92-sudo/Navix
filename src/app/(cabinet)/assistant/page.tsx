import { AssistantContent } from '@/app/(cabinet)/assistant/assistant-content';

export const metadata = {
  title: 'AI Yordamchi',
  description: 'Gapiring — u kerakli xizmatni topib beradi.',
};

export default function AssistantPage() {
  return <AssistantContent />;
}
