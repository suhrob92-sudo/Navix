import { SettingsContent } from '@/app/(cabinet)/profile/settings/settings-content';

export const metadata = {
  title: 'Sozlamalar',
  description: "Shaxsiy ma'lumotlar, til va mavzu sozlamalari.",
};

export default function ProfileSettingsPage() {
  return <SettingsContent />;
}
