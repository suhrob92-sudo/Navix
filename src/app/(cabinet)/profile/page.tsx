import { ProfileContent } from '@/app/(cabinet)/profile/profile-content';

export const metadata = {
  title: 'Profilim',
  description: "Shaxsiy ma'lumotlaringiz va ilova sozlamalari.",
};

export default function ProfilePage() {
  return <ProfileContent />;
}
