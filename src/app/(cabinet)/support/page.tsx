import { SupportContent } from '@/app/(cabinet)/support/support-content';

export const metadata = {
  title: 'Yordam',
  description: "Savolingiz bo'lsa yozing — javob beramiz.",
};

export default function SupportPage() {
  return <SupportContent />;
}
