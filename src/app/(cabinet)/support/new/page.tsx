import { NewTicketContent } from '@/app/(cabinet)/support/new/new-ticket-content';

export const metadata = {
  title: 'Yangi murojaat',
  description: 'Muammoyingizni yozing — qo\'llab-quvvatlash xizmati javob beradi.',
};

export default function NewTicketPage() {
  return <NewTicketContent />;
}
