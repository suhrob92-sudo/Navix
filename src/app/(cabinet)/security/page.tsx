import { SecurityContent } from '@/app/(cabinet)/security/security-content';

export const metadata = {
  title: 'Xavfsizlik',
  description: 'Parolingiz va hisobingiz himoyasi.',
};

export default function SecurityPage() {
  return <SecurityContent />;
}
