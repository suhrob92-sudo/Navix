import { MarketOrdersContent } from '@/app/(cabinet)/marketplace/orders/market-orders-content';

export const metadata = { title: 'Buyurtmalarim — Marketplace' };

export default function MarketOrdersPage() {
  return <MarketOrdersContent />;
}
