import { MarketCartContent } from '@/app/(cabinet)/marketplace/cart/market-cart-content';

export const metadata = { title: 'Savat — Marketplace' };

export default function MarketCartPage() {
  return <MarketCartContent />;
}
