import { DevicesContent } from '@/app/(cabinet)/devices/devices-content';

export const metadata = {
  title: 'Qurilmalarim',
  description: 'Hisobingizga kirgan qurilmalarni boshqaring.',
};

export default function DevicesPage() {
  return <DevicesContent />;
}
