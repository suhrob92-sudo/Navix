import { TaxiContent } from '@/app/(cabinet)/taxi/taxi-content';

export const metadata = {
  title: 'Taksi',
  description: "Manzilni tanlang, narxni oldindan ko'ring va mashina chaqiring.",
};

export default function TaxiPage() {
  return <TaxiContent />;
}
