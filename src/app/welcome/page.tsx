import { WelcomeContent } from '@/app/welcome/welcome-content';

export const metadata = {
  title: 'Xush kelibsiz — Navix',
  description: 'Navix bilan tanishing.',
};

export default function WelcomePage() {
  return <WelcomeContent />;
}
