import { DriverProfileContent } from '@/app/(driver)/driver/profile/profile-content';

export const metadata = {
  title: 'Mashinam',
  description: "Mashina rusumi, rangi, davlat raqami va tarif.",
};

export default function DriverProfilePage() {
  return <DriverProfileContent />;
}
